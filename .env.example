# Required
ANTHROPIC_API_KEY=your-key-here

# GitHub CLI token (needed for PR/blame/size-check via `gh`)
# GH_TOKEN=your-github-token

# Model configuration
# ANTHROPIC_MODEL=claude-opus-4-6

# Git clone directory for analyzed repos
# CLONE_BASE_DIR=/tmp/xray-repos

# Analysis scope (how many months of history to analyze)
# DEFAULT_MONTHS=6

# AI concurrency and timeout
# MAX_CONCURRENT_AI=5
# AI_CALL_TIMEOUT=60

# Limits for PR and blame analysis
# MAX_PRS_CODE_ANALYSIS=30
# MAX_PRS_REVIEW_ANALYSIS=20
# MAX_BLAME_FILES=30

# Diff truncation (characters)
# DIFF_TRUNCATE_CHARS=8000

# Extended thinking token budget for pattern detection
# PATTERN_THINKING_BUDGET=10000

# CORS origins (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000

# --- Production hardening ---

# Max concurrent analysis pipelines (protects CPU/memory/API costs)
# MAX_CONCURRENT_ANALYSES=3

# Reject repos larger than this (MB) before cloning (protects disk)
# MAX_REPO_SIZE_MB=500

# Per-IP rate limit: max analyses per window
# RATE_LIMIT_MAX=5

# Rate limit sliding window in seconds (default: 1 hour)
# RATE_LIMIT_WINDOW=3600
